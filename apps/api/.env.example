# Copier vers .env (non versionné)

DATABASE_URL="file:./dev.db"

# Optionnel : SMTP réel. Sans ces variables, les e-mails sont déposés
# dans apps/api/storage/mail/*.eml et tracés dans le journal d’envoi.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=contact@atelier-nord.fr
